#include "main.h"
/**
 * mul - Multiply two  integers
 * @a: first number to be multiplied
 * @b: seconf number to be multiplied
 * Return: Multiplied values
 */
int mul(int a, int b)
{
	return (a * b);
}
