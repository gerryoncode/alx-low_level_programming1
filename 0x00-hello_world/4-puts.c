#include <stdio.h>
/**
 * main - Entry point
 * puts - prints text on screen
 * Return: 0 if succesful
 * Description: Outputs a string using puts
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
